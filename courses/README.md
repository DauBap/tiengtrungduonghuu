# Course-organized data

This directory is organized as course -> lesson -> content. The original data directories remain unchanged for runtime compatibility.

Each lesson may contain `vocab.json`, `dialogue.json`, `grammar.ts`, `workbook.json`, and `media/`; missing files mean that content is not available for that lesson.
